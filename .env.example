# Next.js app environment variables
# Copy this to .env and fill in your credentials

# Development server port (change if 3000 conflicts with another service)
PORT=3000

# Generate a strong random secret (e.g., `openssl rand -base64 32`)
NEXTAUTH_SECRET=change-me

# Spotify OAuth (create an app at https://developer.spotify.com/dashboard)
SPOTIFY_CLIENT_ID=your-spotify-client-id
SPOTIFY_CLIENT_SECRET=your-spotify-client-secret

# NOTE: NEXTAUTH_URL and SPOTIFY_REDIRECT_URI are built dynamically by docker-compose
# using the PORT variable above and 127.0.0.1 (Spotify requires IP, not localhost)
# Redirect URI in Spotify Dashboard should be: http://127.0.0.1:PORT/api/auth/callback/spotify
# In production, set these explicitly with your production domain.

# ─────────────────────────────────────────────────────────────────────────────
# Usage Analytics (Optional)
# Privacy-first metrics stored locally in SQLite. Disabled by default.
# ─────────────────────────────────────────────────────────────────────────────

# Enable metrics collection (default: false)
# STATS_ENABLED=true

# Path to SQLite database file (default: ./data/metrics.db)
# STATS_DB_PATH=./data/metrics.db

# Salt for hashing user IDs - use a strong random value in production
# STATS_SALT=change-me-to-random-string

# Comma-separated list of Spotify user IDs allowed to view /stats dashboard
# Find your Spotify ID at https://www.spotify.com/account/overview/
# STATS_ALLOWED_USER_IDS=spotify-user-id-1,spotify-user-id-2

# ─────────────────────────────────────────────────────────────────────────────
# Legal Pages Contact Info (Optional)
# Configure your contact details for the Imprint and Privacy Policy pages.
# If not set, placeholder text will be shown.
# ─────────────────────────────────────────────────────────────────────────────

# Your full legal name (required for EU Impressum)
# CONTACT_NAME=John Doe

# Street address
# CONTACT_STREET=123 Main Street

# Postal/ZIP code
# CONTACT_POSTAL_CODE=12345

# City
# CONTACT_CITY=Berlin

# Country
# CONTACT_COUNTRY=Germany

# Contact email address
# CONTACT_EMAIL=contact@example.com