/**
 * Constants for playlist panel virtualization and layout.
 */

/** Height of each track row in pixels (used for virtualization) */
export const TRACK_ROW_HEIGHT = 48;

/** Number of rows to render outside the visible area (overscan) */
export const VIRTUALIZATION_OVERSCAN = 5;
