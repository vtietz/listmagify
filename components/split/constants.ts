/**
 * Constants for playlist panel virtualization and layout.
 */

/** Height of each track row in pixels - normal mode */
export const TRACK_ROW_HEIGHT = 40;

/** Height of each track row in pixels - compact mode */
export const TRACK_ROW_HEIGHT_COMPACT = 28;

/** Height of the sticky table header in pixels - normal mode */
export const TABLE_HEADER_HEIGHT = 40;

/** Height of the sticky table header in pixels - compact mode */
export const TABLE_HEADER_HEIGHT_COMPACT = 32;

/** Number of rows to render outside the visible area (overscan) */
export const VIRTUALIZATION_OVERSCAN = 5;
